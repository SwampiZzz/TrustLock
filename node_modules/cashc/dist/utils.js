import { BytesType, implicitlyCastable, PrimitiveType } from '@cashscript/utils';
import { BinaryOperator } from './ast/Operator.js';
export function resultingTypeForBinaryOp(operator, left, right) {
    if ([BinaryOperator.SHIFT_LEFT, BinaryOperator.SHIFT_RIGHT, BinaryOperator.SPLIT].includes(operator))
        return left;
    if (implicitlyCastable(left, right))
        return right;
    if (implicitlyCastable(right, left))
        return left;
    if (left instanceof BytesType && right instanceof BytesType) {
        return new BytesType();
    }
    return undefined;
}
export function isNumericType(type) {
    return type === PrimitiveType.INT || type === PrimitiveType.BOOL;
}
//# sourceMappingURL=utils.js.map